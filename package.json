{
  "name": "tiktok-bonus-pix",
  "version": "1.0.0",
  "description": "Sistema de saque com PIX PushinPay",
  "scripts": {
    "build": "echo 'Projeto HTML puro - sem build necessário' && node -e \"const fs = require('fs'); const files = ['index.html', 'cadastro.html', 'dashboard.html']; files.forEach(f => { if (!fs.existsSync(f)) throw new Error('Arquivo ' + f + ' não encontrado'); }); console.log('✓ Todos os arquivos HTML verificados com sucesso!');\"",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=18.x"
  }
}
